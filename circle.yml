machine:
  python:
    version: pypy-2.3.1

deployment:
    production:
        branch: master
        heroku:
            appname: slack-foodbot

dependencies:
  override:
    - pip install -r requirements.txt
