machine:
  python:
    version: pypy-2.3.1

deployment:
    develop:
        branch: staging
        heroku:
            appname: staging-foodbot

dependencies:
  pre:
    - pip install PIL --allow-external PIL --allow-unverified PIL
