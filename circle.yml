machine:
  python:
    version: pypy-2.3.1

deployment:
    develop:
        branch: staging
        heroku:
            appname: staging-foodbot

dependencies:
  override:
    - pip install -r requirements.txt
